// Se necesita calcular el area de un circulom que se define como pi * r al cuadrado

const pi = 3.14;
let r = 35;

area = pi*(r**2);

console.log(`El area del circulo es ${area}`);
console.log('El area del circulo es',area);
console.log("El area del circulo es "+area)


// Se necesita calcular la raiz cuadrada y la raiz cubica del numero dado por el area

let rcuadrada = area**(1/2);
let rcubica =  area**(1/3);

console.log(`la raiz cuadrada del area es ${rcuadrada} y la cubica es ${rcubica}`);

// Determinar mediante un true o false si un numero es par

let num = 20;

console.log(num % 2 ==0)



